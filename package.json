{
  "name": "ai-centar-lipik",
  "version": "1.0.0",
  "description": "Repository used for the projects and learning materials related to AI and Python programming at AI Center Lipik.",
  "main": "/scripts/build-static-site.js",
  "type": "module",
  "scripts": {
    "build": "npm install && docker compose up --exit-code-from image-builder && docker compose down && docker compose rm",
    "validate": "npm install && docker compose up --exit-code-from validation-runner && docker compose down && docker compose rm",
    "local:build": "node scripts/build-static-site.js && sudo cp -r dist/* /var/www/html/ai-centar-lipik"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "node-html-parser": "^7.0.1"
  }
}
